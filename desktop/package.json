{
  "name": "yeaboi-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "yeaboi desktop — Electron shell over the yeaboi app backend",
  "type": "module",
  "main": "out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "gen-manifest": "node scripts/gen-routes-manifest.mjs",
    "check-manifest": "node scripts/gen-routes-manifest.mjs --check"
  },
  "dependencies": {
    "preact": "^10.29.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "electron": "^38.2.0",
    "electron-vite": "^4.0.1",
    "typescript": "^5.9.3",
    "vite": "^7.1.12",
    "vitest": "^4.1.10"
  }
}
